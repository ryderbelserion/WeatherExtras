# You never want to commit these 4 folders, .gradle, .idea and build get rather large. jars is just jars, no one needs potentially broken builds.
.gradle
.idea
jars
build

run

!run/config
!run/spigot.yml
!run/bukkit.yml
!run/eula.txt
!run/server.properties
!run/plugins
!run/ops.json